plugin.tx_project {
    view {
        # cat=plugin.tx_project_pi1/file; type=string; label=Path to template root (FE)
        templateRootPath = EXT:project/Resources/Private/Templates/
        # cat=plugin.tx_project_pi1/file; type=string; label=Path to template partials (FE)
        partialRootPath = EXT:project/Resources/Private/Partials/
        # cat=plugin.tx_project_pi1/file; type=string; label=Path to template layouts (FE)
        layoutRootPath = EXT:project/Resources/Private/Layouts/
    }
    persistence {
        # cat=plugin.tx_project_pi1//a; type=string; label=Default storage PID
        storagePid =
    }
    settings{
    	scss = EXT:project/Resources/Public/Sass/_project.scss 
    }
}
